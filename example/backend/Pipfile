[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.68.0"
uvicorn = ">=0.15.0"
pydantic = ">=1.8.0"
typing-extensions = ">=4.0.0"
sqlalchemy = ">=1.4.0"
python-multipart = ">=0.0.5"

[dev-packages]

[requires]
python_version = "3.12"
