import { DimensionsSettings, PositionSettings } from '../../CommonSettings';

export const WorldMapSettings = () => {
  return (
    <>
      <DimensionsSettings />
      <PositionSettings />
    </>
  );
};
